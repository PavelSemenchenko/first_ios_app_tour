# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'app_tour' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'Alamofire'

  # Pods for app_tour

  target 'app_tourTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'app_tourUITests' do
    # Pods for testing
  end

end
